import React from 'react';
import {Text, View} from 'react-native';

const PairedDeviceContainer = () => {
  return (
    <View>
      <Text>PairedDeviceContainer</Text>
    </View>
  );
};

export default PairedDeviceContainer;
